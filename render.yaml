services:
  - type: web
    name: digital-fatigue-api
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn digital_fatigue.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.9.0 