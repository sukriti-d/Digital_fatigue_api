services:
  - type: web
    name: digital-fatigue-api
    env: python
    buildCommand: pip install -e . && pip install -r requirements.txt
    startCommand: uvicorn digital_fatigue.main:app --host 0.0.0.0 --port $PORT --workers 1
    envVars:
      - key: PYTHON_VERSION
        value: 3.9.0 